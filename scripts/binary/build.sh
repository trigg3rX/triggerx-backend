#!/bin/bash

# Run with ./scripts/binary/build.sh <optional_version>
# if ! [[ "$0" =~ scripts/binary/build.sh ]]; then
#   echo "must be run from repository root"
#   exit 1
# fi

# go build -v -o ~/bin/triggerx cli/main.go